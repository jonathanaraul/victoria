{% extends "ProyectoFrontBundle:Helpers:body.html.twig" %}

{%block imagenes%}
var images = ["{{ asset(''~ background ~'') }}"];
{%endblock%}

{%block contenido%}
<div class="middle-right-sub">
	{{ render(controller('ProyectoFrontBundle:Helpers:menuMobile')) }}
	<div class="upper-text" style="background-color: {{theme.color}} !important"></div>
	<div class="news-cont">
		<hr>
		<p class="content-title">
			Reservaciones
		</p>
		<div class="news-cell">
				{%if message == ''%}
				<form action="{{ url }}" method="post" {{ form_enctype(form) }}>
					<table style ="width: 100%;">
						<tr>
							<td>
								Nombre Completo:
							</td>
							<td>
								{{ form_widget(form.name, { 'attr': {'style': 'width: 100%;'} }) }}
							</td>
						</tr>
						<tr>
							<td>
								Email:
							</td>
							<td>
								{{ form_widget(form.email, { 'attr': {'style': 'width: 100%;'} }) }}
							</td>
						</tr>
						<tr>
							<td>
								Telefono:
							</td>
							<td>
								{{ form_widget(form.phone, { 'attr': {'style': 'width: 100%;'} }) }}
							</td>
						</tr>
						<tr>
							<td colspan="2">
								<input type="submit" name="save" value="Guardar">
							</td>
						</tr>
					</table>
				</form>
				{%else%}
				<p>{{message}}</p>
				{%endif%}
		</div>
		
		<div class="lower-text"></div>
		<div class="lower-box" style=""></div>
	</div>
	<div class="main-big-title news-big-title" style="">
		<div class="theme-sel-2">
			Reservaci&oacute;n
		</div>
	</div>
</div>
{%endblock%}

{%block celda%} <div class="cell-right-sub"></div>
{%endblock%} 